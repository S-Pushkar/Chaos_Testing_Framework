package master.com.chaos_testing_framework.service;

public class DockerService {
    // runs every x seconds -> iterate through the configs -> for each config, choose random service associated with config and choose a random fault and inject it

    // runs every 1 second -> inspect all the running containers in your laptop => maintain 5 minute window

    // runs every 5 minutes -> deletes the container stage existing beyond 5 minute window

    
}
